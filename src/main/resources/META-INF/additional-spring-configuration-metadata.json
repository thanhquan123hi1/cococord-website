{
  "properties": [
    {
      "name": "spring.data.redis.enabled",
      "type": "java.lang.Boolean",
      "defaultValue": false,
      "description": "Application-level toggle used to conditionally enable Redis-backed presence features (custom property)."
    }
  ]
}
